## ---- include = FALSE---------------------------------------------------------
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)

## ----setup--------------------------------------------------------------------
library(HistDat)

## -----------------------------------------------------------------------------
h = HistDat(
  vals = 1:4,
  counts = c(1e12, 2e12, 2e12, 1e12)
)

## -----------------------------------------------------------------------------
mean(h)
min(h)
length(h)
median(h)

## -----------------------------------------------------------------------------
h = HistDat(
  vals = 1:4,
  counts = c(1, 2, 2, 1)
)
as.vector(h)

